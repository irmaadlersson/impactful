import React from 'react';

// Initialize the properties and functions (getters + setters) that you need to provide
export default React.createContext({
  setSomething: () => {},
  something: 'something'
});
